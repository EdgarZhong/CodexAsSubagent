# 20260912 21:34 — Codex As Subagent × Kimi Code 真实环境 E2E 验证（10 工具 + Hook 回流）

## 背景

用户要求端到端验证 Codex As Subagent 在本机 Kimi Code（TUI）中是否完全就绪：覆盖 10 个 MCP 工具调用与主动回流机制，确保每个 hook（PreToolUse / Stop / UserPromptSubmit）各种行为都测到。授权使用真实 Codex subagent（GPT-5.6 Luna，effort low）。

- 会话：`731af287-a122-4868-a763-4724248fa3fb`（重启 resume 后沿用）
- 真实库：`~/.codex-as-subagent/state.sqlite`（V2 schema）
- 插件缓存：`~/.kimi-code/plugins/managed/codex-as-subagent/`

## 关键结论

1. **10 个 MCP 工具全部真实调用通过**（见矩阵）。
2. **发现并修复第二个 hook 链路 bug**：宿主（Bun）`spawn(command, [], {shell:true})` 对「node 绝对路径 + 多参数」命令字符串静默失效（无进程痕迹、fail-open allow），但对 `sh /abs/script.sh` 形态可靠。修复：安装器生成 `hook-wrapper.sh`，hook 命令统一本地化为 `sh <installPath>/hook-wrapper.sh`。
3. **矩阵探针污染真实库**：current_sessions 残留 `sess-probe`，导致本轮 spawn 的 completion 归属错误（session_id=sess-probe），回流按 session 匹配不上而不注入。已修复归属并恢复 current_sessions。
4. **回流机制经手动模拟宿主调用验证核心语义正确**（drain → `<codex-completion>` → exit 2）；宿主内调用待第四次重启后确认。
5. **`codex_wait_many` threads 数组分支在当前模型下不可用**（`"all"` 分支正常）——归因已修正（2026-09-13 源码级排查）：**非宿主限制**。宿主校验为 Ajv（对真数组放行，实验实锤）、出站 schema 全 provider 直发、入站链路无串化点；串化发生在模型生成的 tool_call arguments（模型在 anyOf 形状上的行为）。裁决不改 schema；备用修复方案已定稿（threads 加非空字符串分支 + server 端 JSON.parse 还原），按需启用。

## 修复 1：Hook 命令形态（sh wrapper）

### 证据链

| 实验 | 结果 |
| --- | --- |
| 带引号 node 命令（9/11 版 manifest） | 20 秒高频 ps 监控零进程痕迹；无任何 hook 效果 |
| 无引号 touch 探针（前次会话） | 触发成功（/tmp 标记 mtime 实锤）→ 宿主能解析 manifest hooks |
| 无引号 node 多参数命令（21:02 版 manifest） | 第三次重启后仍零效果：current_sessions 未被更新、pending completion 历经 5 次 MCP 调用未 drain |
| knowledge-mem 对照 | 同结构 manifest hooks（Stop/SessionEnd/PreCompact）在同一重启周期内正常触发（nowledge-mem-hook.log 21:04:45–21:05:05）→ 宿主 hook 分发机制存活 |
| 手动模拟宿主执行 hook 命令（printf stdin JSON \| node main.mjs hook --host=kimi-code） | 完全正常：drain probe-thr-a → `<codex-completion>` 输出 → exit 2 block 语义正确 → **命令健康，问题在宿主对该命令形态的执行** |
| 宿主二进制逆向 | `runHook`: `hostProcess.spawn(command, [], {shell:true, cwd, env})`，失败静默 `allowResult`；`buildSpawnOptions`/`waitForSpawn` 无异常；`runMatchedHooks` 按 matcher/cwd+command 去重后 `Promise.all` 执行 |

### 修复内容

- `src/install/kimi-code-plugin.mjs`：新增 `HOOK_WRAPPER_FILENAME` / `buildHookWrapper()`；`localizeKimiHooks(manifest, { installPath })` 把所有 hook command 统一改写为 `sh <installPath>/hook-wrapper.sh`；install 时写 wrapper（mode 755），内部 `exec <node> <cli> hook --host=kimi-code "$@"`（标准 sh 引号语义，stdin 经 exec 透传）。
- 测试：`tests/unit/install-kimi-code-plugin.test.mjs` 更新 + 新增「真实 sh 执行 wrapper 到达 hook CLI」端到端断言（execFileSync + 真实 node，54ms 通过）。
- 验证：npm test 220/220 fail 0；npm run lint；npm run smoke；install --host=kimi-code 重装后产物 manifest 与 wrapper 已确认。

## 修复 2：真实库探针污染清理

- `current_sessions`：`sess-probe` → `731af287-…`（UPDATE current_sessions，恢复唯一真源）。
- `completions`：本轮 4 条真实 completion（01a095bd completed / 01a095c6-0e37 completed / 01a095c6-0db1 interrupted / 01a095c7-259f completed）的 session_id 从 `sess-probe` 更正为 `731af287-…`（错误归属源于 Runtime 从被污染的 current_sessions 读取）。
- `probe-thr-a` 探针行：经手动 hook 实验按真实链路 drain → delivered（`<codex-completion>` 已在实验中消费）。

## 10 工具矩阵（全部真实调用）

| # | 工具 | 输入 | 结果 | 证据 |
| --- | --- | --- | --- | --- |
| 1 | codex_models | - | PASS | 默认 gpt-5.6-luna/xhigh；5 个模型含 gpt-6-astra |
| 2 | codex_spawn | prompt(137*42), gpt-5.6-luna/low | PASS | threadId 01a095bd 立即返回 running；异步语义正确 |
| 3 | codex_status | 01a095bd / B / C | PASS | completed/running/interrupted 三态均正确；含 idleForSec、latestAction |
| 4 | codex_list_threads | - | PASS | total 7，含本轮全部线程 |
| 5 | codex_read_thread | 01a095bd | PASS | assistantMessages 两条（"5754…" 与 send 后的 "96 / 8 = 12。"）；changes 空数组 |
| 6 | codex_send | 01a095bd + prompt | PASS | 返回 running；完成后 read_thread 可见新回答 |
| 7 | codex_steer | B(sleep 45) + 追加指导 | PASS | `{accepted:true,status:"running"}` |
| 8 | codex_interrupt | B | PASS | interruptRequested ACK → status=interrupted |
| 9 | codex_wait | 01a095bd | PASS* | 返回 completed + finalAssistantMessage + changes；*Minor：final message 取到旧轮（首轮）回答而非最新轮 |
| 10 | codex_wait_many | threads:"all" | PASS* | 等待调用瞬间全部 active threads：E/F 两个 sleep 任务返回 completed + 真实 shell 输出（E-done/F-done）；*Important：数组分支 `threads:["id",…]` 报 schema 错误——归因已修正（源码级排查）：模型生成 tool_call 时把数组串化（宿主 Ajv 校验对真数组放行、本地链路无串化点），非宿主限制 |

工具调用均真实触发宿主 PreToolUse hook 分发点（security-check.sh 同进程路径），且 MCP 通道与 current_session 解析正常（`#session` 在 ctx 无 sessionId 时读 current_sessions；污染期间 sess-probe 被误认作当前会话——已清理）。

## Hook 行为矩阵（29/29 已由离线矩阵覆盖 + 本轮实测补强）

离线矩阵 `cd /tmp/cas-hook-e2e && node hook-matrix.mjs`：29/29（门禁四分支、三事件回流、fail-open/closed、孤儿 claim 恢复、lease 保护）。

本轮实测新增证据：

- **PreToolUse 回流优先**：手动宿主式调用，pending probe completion → `<codex-completion>` 注入 stderr + exit 2，且注入后不执行门禁（hook.mjs ①→②顺序）。
- **exit 语义**：exit 2 + stderr（block 送达）；exit 0（无 pending 时放行）；未知 session + UPS → fail-open exit 0（单测）。
- **回流按 (host, workspace, sessionId) 三元组 claim**：sess-probe 归属的 completion 对真实会话不可见（反向证明谓词生效）。

## 待重启后验证（第五次重启，source 枚举修复后）

**——已于 2026-09-13 晨全部完成，见决策 15 与下方"最终验证"——**

## 第四次重启后的判别记录（2026-09-13 晨）

- 5 条 pending 未 drain、`kimi -p` 全新进程调 codex_models 成功但 current_sessions 无痕迹、`-p` 进程 systemPrompt 正常注入、宿主 hook 分发机制活着（knowledge-mem SessionEnd 在第四次重启时正常触发）→ 锁定插件 record 层 → 用户 `/plugins` 报错实锤 source 枚举根因（决策 14）。

## 最终验证（2026-09-13 09:32–09:42，真实宿主全链路首次打通）

1. **source 修复生效**：`/plugins` 面板不再报错，Codex As Subagent `enabled | state: ok | local-path`，详情页无 Diagnostics（hooks 的 zod 校验全过；详情页不渲染 Hooks 区块属宿主渲染遗漏——knowledge-mem 同样不显示但 hooks 工作）。
2. **R reload 激活 hooks**：面板 R 键重载后，wrapper 裸观测日志开始留痕（`/tmp/cas-wrapper.log`），PreToolUse touch 探针留痕——宿主真正开始执行我们的 hooks，无需重启 TUI。
3. **真实 stdin 实锤对齐双 bug**：wrapper 抓到宿主 payload `session_id="session_731af287-…"`（带前缀）、`tool_name="mcp__codex-as-subagent__codex_models"`（限定名）——drain 谓词与 gate 集合双双失配（决策 15①②）。
4. **修复与验证**：清库统一 session 形态 + `src/cli/hook.mjs` `casToolName()` 归一化（hook-gate 单测，28/28；全量 220+/220+ fail 0）+ 重装恢复正式产物；hook CLI 每事件现起读源码，修复即时生效。
5. **真实回流首次触发**：TUI 内 spawn `01a0986d`（验证任务 11+22）→ 下一工具调用被宿主 veto + `<codex-completion>` 注入（"11 + 22 = 33。"送达模型上下文）→ 重试原调用，查库 `delivery_state=delivered`、`current_sessions.session_id=session_731af287-…`（hook gate 建立）。
6. **手动宿主式调用回归**：一次注入 5 条积压 completion（completed/interrupted 混合），drain→envelope→exit 2 语义与离线矩阵一致。
7. **清理**：/tmp 全部探针文件与矩阵测试目录经 `/usr/bin/trash` 移除；重装覆盖临时探针 manifest/wrapper。

**结论：Codex As Subagent 在本机 Kimi Code 宿主就绪**——10 工具矩阵、PreToolUse 双职责（回流+门禁）、Mailbox drain/delivery 全部在真实用户路径验证。Stop hook 的 drain 语义与 PreToolUse 同通道（hook.mjs 同一代码路径），本轮会话结束时自然发生，无需单独构造。

## 观察项 Ledger

- Minor：`codex_wait` 的 finalAssistantMessage 返回线程首轮而非最新轮回答（数据完整，read_thread 可见全量）。
- Important（归因已修正）：`codex_wait_many` threads 数组分支在当前模型下不可用；`"all"` 分支可用。源码级排查实锤：宿主 Ajv 校验对真数组放行、本地链路无串化点，串化发生在模型生成的 tool_call arguments。裁决不改 schema；备用修复方案已定稿（threads 加非空字符串分支 + server 端 JSON.parse 还原），按需启用。
- Important（宿主侧）：Bun shell 对 hook command 的解析限制——需 `sh wrapper.sh` 形态（本次修复）。
- 注意：`installed.json` 无 hooks 数量视图；`kimi plugins list` CLI 子命令已不存在（AGENTS.md SOP 需更新表述）。

## 变更文件

- `src/install/kimi-code-plugin.mjs`（wrapper 生成 + localize 签名）
- `tests/unit/install-kimi-code-plugin.test.mjs`（断言更新 + wrapper 端到端测试）
- 本文件 + CLAUDE.md 看板
