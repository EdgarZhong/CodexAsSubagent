# CLAUDE.md

## 当前阶段

- 目标：V2 升级已全部交付——三份 V2 规格实现、确定性回归与**双宿主（Kimi/ZCode）真实用户级验收全部通过**（2026-09-13）；剩余 Backlog：开源一键安装在 Host 子进程 PATH 的发现方案、`status=failed` 路径构造验证。
- 开源发布（2026-09-16，用户授权）：README 双语重写为面向用户口径（`README.md` 英文主 + `README.zh-CN.md` 中文镜像互链），定位改为「any coding-agent harness 的 Codex subagent runtime」，Roadmap 三级（当前 Kimi/ZCode → Claude Code 新 Mod 范式 + Pi → DSH → Grok Build/Gemini CLI）；新增 `setup.sh`（Node≥24 检查 + 子模块 + npm install + doctor），Quick start 定为 clone → setup.sh → `install --host=<host>`；仓库设为 GitHub 公开，英文简介。release 渠道后续候选：npm 全局安装（vendor submodule 需 prepack 打包）、Host 市场路线后置。
- 权威规格（冲突时以此为准，均为 2026-09-12 修订后的唯一自洽版本）：
  1. `docs/CodexAsSubagent V2 Session 隔离与 Mailbox 架构设计.md`
  2. `docs/CodexAsSubagent V2 串投修复与 Kimi Code 集成适配说明.md`
  3. `docs/CodexAsSubagent V2 Host 隔离、Thread Hold 与 CLI-Adapter 实施规格.md`
- V1 详细设计已降级为基线规格（`docs/Codex As Subagent — 详细设计与编码规格.md`，被取代小节已移除）；V1 已全部交付并通过用户级验收，证据见 docs/autonomous-runs/。
- 执行方式（用户 2026-09-12 指令）：自主实现流程中**独立 Review Agent 取消**，全部复核与验收由主会话承担；subagent 只负责必要的 explore 与实现；使用 ZCode 原生子代理，不使用 Codex。
- 环境事实：本机 Codex runtime 为 ChatGPT.app 内嵌 0.154.0-alpha.6.2；确定性回归基线 223/223（2026-09-13，含 thread_held 投影修复）。

## V2 任务看板

- [x] 三份 V2 规格按最终修订指令合并为唯一自洽版本（含 `current_session` 持久化、破坏性重建、Runtime 内部 Web delivery、Fail-Closed/Recovery Matrix、Host 产品定义；Doc A 改名去 "(1)"）。
- [x] V1 详细设计精简为基线规格；Kimi 知识库加 V2 状态标注；三份核心文档同步。
- [x] T1 SQLite V2 schema 重建与 Host-scope Store（executions/completions + host/session_id，thread_holds、host_presence、current_sessions；claim/ack/nack/gate 全谓词 host 化；破坏性重建，schema_version='2'）。
- [x] T2 Runtime core 隔离逻辑（SessionContext 解析与 `session_not_established`、send A–F Hold 算法、新 Hold 后失败按 hold_id 释放、steer/interrupt/wait/status/read_thread/list_threads 的 `thread_held` 语义、provenance 继承/显式透传、recovery 修复 current_session 且不重置 Presence/Hold）。
- [x] T3 Host Protocol Registry + CLI 协议（src/hosts/{registry,kimi-code,zcode}；hook `--host` 必填 + PreToolUse 双职责固定顺序 + gate 异常 fail closed；drain 三参必填；mcp `--host` 必填 + Presence 注册后处理调用、20s 直写 heartbeat、退出 detach；`delivery.nack` 接线）。
- [x] T4 Kimi Web 事件驱动投递（已被同日收口轮撤销，见下条；legacy kimi-web CLI/worker/registry 移除并归档 .archive/v2-removed/）。
- [x] T4 收口（2026-09-12 下午，Hook-only Mailbox 定稿）：删除全部新旧 Web transport（web-delivery/kimi-web-client/serve 接线/`multiple_active_host_servers`，归档 .archive/v2-removed/）；Completion 四态 `pending/claimed_waiter/claimed_hook/delivered` + `claim_id`/`claimed_at` 全链路 CAS；Terminal Initiate 两分支（Waiter/Normal Initial Transaction）；claim 层通用过期恢复（Hook session 级 + waiter thread 级，无启动 sweep）；`reservation_kind` 'direct'→'waiter'；内部 RPC `deliveryId`→`claimId`；session_id opaque 核查通过；新增迟到 ACK/NACK 隔离等 3 组竞态测试；回归 212/212。
- [x] T5 Kimi 插件资源与安装器（hooks 恰为 PreToolUse/Stop/UserPromptSubmit；MCP 注册 args `["mcp","--host=kimi-code"]`；README/SYSTEM 更新）。
- [x] T6 主会话集成与回归：serve 接线、install 文案；真实 SQLite 端到端探针（隔离/门禁/接管/回流/跨 session 不串投）通过；验收记录 docs/autonomous-runs/20260912-1610-v2-host-session-isolation.md（T4 收口轮另立记录）。
- [ ] 用户级验收：真实 Kimi TUI E2E 由用户执行（重装插件 → 重启宿主 → TUI PreToolUse 回流；MCP 验证 `--host=kimi-code`；Web 端当前仅验证 MCP 正常注册，无 Web 投递路径）。
- [x] ZCode 插件集成（2026-09-12 晚，收口后单独轮）：adapter 契约补齐（`sessionGateEvent='PreToolUse'`、`blockDeliveryEvents` 空集、新增 `deliveryEvents` 字段——zcode 的 PreToolUse 仅门禁不做回流）+ hook.mjs 回流按 deliveryEvents 门控；`plugins/zcode` 资源 V2 化（`.mcp.json` 加 `--host=zcode`、hooks.json 增 PreToolUse 门禁、README 修"没有 Hook"全局承诺为 per-Host 口径）；安装器 `localizeMcpConfig` 保留源 args；新增 6 个 zcode 用例，回归 217/217。真实 ZCode E2E 待用户后续验证。
- [x] Kimi E2E 准备（2026-09-12 晚）：`install --host=kimi-code` 重装 V2 插件到宿主并验证注册（用户级 mcp.json 携带 `--host=kimi-code`、hooks 恰为 PreToolUse/Stop/UserPromptSubmit）；真实会话测试由用户执行。
- [x] 用户级验收：真实 Kimi 会话 E2E（2026-09-13 晨收官）——**10 个 MCP 工具真实调用全通过**（spawn/status/list/read/send/steer/interrupt/wait/wait_many:all/models；证据 docs/autonomous-runs/20260912-2134-kimi-e2e-10工具与hook回流验证.md）；三层根因逐级修复（决策 12→13→14→15：source 枚举 → sh wrapper 稳妥形态 → session_id 前缀与 tool_name 限定名对齐）；**真实宿主全链路首次打通**：PreToolUse gate 建立 current_sessions（session_ 前缀形态）→ completion 正确归属 → 下一工具调用 veto + `<codex-completion>` 注入 → delivered 落库。遗留观察项：wait 数组分支（宿主序列化）、宿主详情页不渲染 hooks 数量、Stop hook 真实触发留待本轮会话结束自然发生（drain 语义同 PreToolUse 已验证）。
- [x] 用户级验收：双活跃会话 Session 隔离补充 E2E（2026-09-13 上午，Web+TUI 同 workspace 并发）——A/B 两相全通过：Web 会话真实 spawn 回流闭环（veto+注入+delivered）；TUI 身份 hook 不消费 Web pending completion（drain session 谓词隔离）；TUI 真实切走 current_session 后 Web CAS 调用 gate 懒切换归属正确。机理确认：gate 仅 CAS 工具写 current_sessions、completion 归属 spawn 时刻定、回流 claim 用 stdin 原样 session 不读 current_sessions、**gate 层运行互斥**（对方 session 有 active execution 时本会话 CAS 工具全部 veto，turn 结束 session_handoff 自动交接；实测 C1–C3 通过）。已知限制登记：双会话并发 CAS 的毫秒级 gate/dispatch 竞态（约束边缘，不修复）、运行中线程 session 围栏完整（send→thread_busy、steer/interrupt→session_conflict、跨 host→thread_held；idle 线程 lazy takeover 为设计语义，非抢占）。证据 docs/autonomous-runs/20260913-1050-双活跃会话session隔离补充e2e.md。
- [x] ZCode 侧临时停用恢复与 source 风险取证（2026-09-13）：enabledPlugins 开关翻回 true 装回（缓存/注册表全程未动）；逆向 ZCode 宿主 bundle（asar → /out/host/index.js）取证 `isInstalledPluginRecord` 仅校验 id/installPath/name 三字段为 string，`source` 不在校验范围、无 "output validation failed" 枚举层——**决策 14e 的同构风险在 ZCode 侧不成立**，`source: "plugins/zcode"` 记录安全，zcode 安装器无需修改。回归 221/221（含决策 14/15 未提交修复）、doctor PASS（codex 0.154.0-alpha.6.2）、状态库已是 V2 schema。
- [x] 用户级验收：真实 ZCode 会话 E2E（2026-09-13）——**全通过**：4 线程真实模型执行（gpt-5.6-luna/xhigh）；PreToolUse gate 建立 current_sessions（session_id 为 ZCode 原样 `sess_` 前缀形态，两 host 会话行并存互不干扰）；PostToolUse 回流 ×4 + UserPromptSubmit 回流 ×2（注入形态为宿主原生 additionalContext 块，内容完整含 500/1500 行全量输出），四态状态机 claim→delivered 全部落库（含 interrupted 终态）；错误面 send→thread_busy、steer→no_active_turn 符合设计；list_threads/status/interrupt 正常。Stop 真实触发依赖最后工具调用与回合结束间的亚秒时序窗口（结构上难稳定命中），与 Kimi 轮同口径登记。证据 docs/autonomous-runs/20260913-1305-zcode真实会话e2e与跨host隔离.md。
- [x] 用户级验收：跨 Host 隔离真实 E2E（2026-09-13，Phase A/B/C 三相全过）——A：kimi 探测 zcode-held 运行中线程 6 操作全 thread_held，库证 gate 懒切换 + completion 无跨 Host 泄漏，**附带发现 thread_held 投影丢 data bug（决策 16，已修复 223/223）**；B：修复后 zcode 探测 kimi-held 线程 6 操作全 thread_held 且带 `data.holderHost:"kimi-code"`（真实路径复验修复）；C：全 kimi presence 过期后 zcode `codex_send` 跨 Host lazy takeover 成功（hold CAS 换主、execution 归属本会话、completion 回流 delivered），同线程双 turn 归属对照实证 spawn 时刻归属语义（孤儿 completion 登记为已知语义）。

### Backlog（承接 V1 未完成项）

- [ ] `tests/e2e/`、`tests/fixtures/` 空占位：缺可重复执行的自动化 E2E。
- [ ] 开源一键安装在 Host 子进程 PATH 的发现方案（ZCode 插件 update 重同步覆盖命令本地化）。
- [ ] 真实 `status=failed` turn 路径未单独构造验证。

### 明确不做（V2 决定，非遗漏）

- **多 Server 路由与 Web Push**：Server 型 Host 单 active Server 为环境假设（无专用错误面）；同一 `(host, workspace, session_id)` 单活跃 interactive instance 为产品约束。当前 V2 唯一主动 delivery transport 是 Hook；Web Push 未来按新版设计重做，不以旧代码为地基。
- **V1→V2 数据迁移**：不做 migration，不保留旧 CAS runtime state（含 delivered history），不实现 `.bak-v1` 自动备份。
- **不做线程锁探测**；`thread_locked` 仅覆盖 "already has an active writer" 归一（V1 决定继续有效）。
- **不新增 Hook 事件**：仍固定 `UserPromptSubmit` + `PostToolUse` + `Stop`（ZCode）；Kimi 为 `PreToolUse` + `Stop` + `UserPromptSubmit`。
- **MCP public API 不增加**：V2 不改 10 个工具的 names、input schema、业务语义与调用方式；Host/Session 隔离属于内部 Runtime context，不进入模型可见 MCP 参数。
- **Hook 不做重活 / 不以 PID 相等清 orphan app-server**（AGENTS.md，继续有效）。

## 继承决策（V1 定稿、V2 继续有效）

1. 技术栈：Node.js 24 ESM + node:sqlite；vendor/codex-supervisor-mcp 以 Submodule 固定，业务代码只经 src/adapters/supervisor/ 访问。
2. MCP transport：stdio Bootstrap + Unix socket Runtime 分层；最小 JSON-RPC façade；Bootstrap 不持有 Runtime/SQLite 业务状态。
3. Runtime 自动发现：`CODEX_BIN` > PATH > standalone managed > `~/.local/bin` > Homebrew > ChatGPT.app > 旧 Codex.app；realpath 去重 + `--version` + schema method 检查 + ephemeral initialize 冒烟；单生命周期不切换 binary。实现于 `src/shared/codex-runtime.mjs`。
4. 配置分层：`~/.codex-as-subagent/config.toml` 是对 Codex 配置的可选增量覆写（`-c key=value` 注入 app-server 前）；Codex 二进制走 `CODEX_BIN` env，不写 config.toml。实现于 `src/shared/codex-runtime.mjs` + `src/cli/serve.mjs`。
5. 兼容性事实：app-server 必须始终显式设置 `CODEX_APP_SERVER_ARGS`（至少 `["app-server"]`）；`turn/completed` 通知复用承载 completed/interrupted/failed（以 canonical turn record status 细分）。
6. 错误规范化：仅 `already has an active writer` → `thread_locked`（Codex writer-lock 层）；V2 新增 `thread_held`（CAS Host Hold 层，含 `data.holderHost`），两层严格分离，不得合并或把 Codex 表示成 holder。
7. 回流事件：ZCode 固定 UserPromptSubmit/PostToolUse/Stop；PostToolUse 省略 matcher 匹配所有工具、不接受 decision；Stop 以 `decision:block` 续轮；不加 `stop_hook_active` 护栏；双通道经 claim 状态互斥。
8. ZCode Hook 协议事实（逆向取证）：恰好 7 事件；stdout 必须以 `{` 开头严格 JSON；Hook env 注入 `ZCODE_SESSION_ID`（session 身份只在 Hook 通道可得）；MCP 进程 env 无 session 变量。取证见 docs/research/2026-09-11-zcode-hook-protocol.md。
9. Kimi Hook 协议事实：stdin JSON `{hook_event_name, session_id, cwd, ...}`（session_id 只在 stdin 字段）；exit 0 allow / exit 2 block；仅 PreToolUse/Stop/UserPromptSubmit 可影响主流程；Hook 对 MCP 工具调用也触发。协议依据见 Kimi 知识库（协议事实部分仍有效）。
10. 插件安装：`plugins/<host>/` 是唯一真源，安装器拷贝+本地化+注册；Kimi MCP 注册到用户级 `$KIMI_CODE_HOME/mcp.json`（宿主以 workspace cwd 拉起），插件 manifest 禁止携带 MCP；K2.7 模型 `kimi-code/kimi-for-coding` 为 Kimi 产品协议事实（当前版本无 Web 投递路径）。
11. V1 串投取证（已由 V2 结构性修复）：host 谓词缺失、session 维度缺失、detached worker 孤儿化；证据见 docs/autonomous-runs/20260911-2320-session-routing-and-isolation-findings.md。

## V2 动态决策（2026-09-12 定稿）

1. **五项裁决**：(a) ZCode 本轮实现 Core Adapter 与全部 Host 维度逻辑，`plugins/zcode` 资源与真实 E2E 推后，旧 ZCode 插件暂时不可用为接受的阶段性状态，禁止为兼容而给 `--host` 默认值/保留 plain/猜 Host/workspace-only；(b) `current_session` 持久化为 SQLite `current_sessions` 表（主键 `(host, workspace)`），是 persisted routing state；(c) 无 `current_session` 时 MCP 调用 fail closed `session_not_established`（self-recovering）；(d) ~~Kimi Web 回流为 Runtime 内部事件驱动 delivery~~（已被 T4 收口撤销：当前 V2 只有 Hook 回流，Web Push 整体移除）；attach/worker/polling 全部废弃；(e) V1→V2 破坏性重建，不做 migration。
2. **权威优先级**：active Execution（lifecycle truth）> `current_session`（routing state）> Thread Hold（control state）；stale routing/hold 状态不构成占用；conflict 一律为派生状态，durable facts 收敛后自动恢复（Recovery Matrix 见架构设计 §十）。
3. **Thread Hold**：`thread_holds`（thread_id 主键）+ `host_presence`（(host, workspace, instance_id) 主键，heartbeat 20s / lease 60s，MCP Bootstrap 直写 SQLite，不经 Runtime Server RPC）；Hold 无独立 lease，有效性由 Presence + active Execution 判定；取得入口仅 spawn/send；lazy takeover 原子竞争；新 Hold 后 startTurn 失败只按本次 hold_id 释放。
4. **线程操作语义**：send 按 A–F 判定算法（thread_busy/thread_held/lazy takeover）；steer/interrupt 不 takeover；wait 不取 Hold、对他 Host active Execution 报 `thread_held`；status/read_thread 对其他 active Host 有效持有的 Thread 报 `thread_held`；list_threads 只列 caller 持有 + free + stale-held idle。
5. **claim 谓词（T4 收口修订）**：Hook/waiter claim 一律 `(host, workspace, session_id, delivery_state='pending')`；Hook 缺 session_id 拒绝 claim 不得 fallback；四态 `pending/claimed_waiter/claimed_hook/delivered`，ACK/NACK/release 以 `delivery_state + claim_id` 双条件定界（`WHERE host = ? AND claim_id = ?`），每次重新 claim 生成新代际；claim 层过期恢复先于消费 CAS（Hook session 级 / waiter thread 级，基准 `claimed_at`，无启动 sweep）。
6. **CLI 协议**：`serve` 禁止 `--host/--workspace/--session`；`mcp --host` 必填、 Presence 注册成功后才处理调用、退出 best-effort detach；`hook --host` 必填、移除 plain fallback 与 `--workspace` override；`drain --host/--workspace/--session` 三项必填。
7. **Host 身份**：Host 是产品类型（kimi-code/zcode/...），由 Host integration 静态指定，不得从 cwd/session/进程名/工具参数推断；未知 Host 在任何 CAS 状态读写前 `UnknownHostError` fail closed；TUI/Web 统一 `kimi-code`，`kimi-code-web`/`kimi-code-tui` 禁止。
8. **文档裁决**：三份 V2 文稿为唯一权威，V1 设计降级基线；`src/hosts/<host>.mjs` 是 Host-native 字段唯一出现处，`cli/hook.mjs` 不直接读 Host-native 字段；禁止以 legacy 代码反推产品设计，代码与规范冲突时改代码。
9. **实现边界裁决（2026-09-12，逐条证据见验收记录 §五）**：Hold 接管为"快照读 → 事务外 presence 判定 → CAS 写 + 冲突重读"；Gate 空闲竞争按懒切换语义（先后 allow）；heartbeat UPDATE-only（`refreshed:false` 由调用方重挂）；`resumeThread` 失败同样释放本次新 Hold；无法确定唯一 holder 的 `thread_held` 不带 `data.holderHost`；provenance 显式事件值优先、行一致性校验 fail closed；gate 求值失败 veto（fail closed）、回流渲染失败 fail-open；kimi-web legacy 文件移入 `.archive/v2-removed/`。
10. **T4 收口六项裁决（2026-09-12，用户逐条确认）**：(a) Web delivery 新旧实现全部删除归档，不区分旧旁路与上轮新实现；(b) 孤儿 claim 用 lease recovery（`claimed_at`+timeout），下沉为 claim 层通用机制（Hook claim 与 waiter claim 消费前先回收），不加 Runtime 启动 sweep；(c) 删除 `multiple_active_host_servers`（Session 级单活跃实例约束独立成立）；(d) `claimed_waiter` = waiter 持有消费权（不分 terminal 前后），出生态仍只有两分支；(e) reservation 载体维持 executions.reservation_*，不新增表，kind 值 'direct'→'waiter'；(f) 内部 RPC 参数 `deliveryId`→`claimId`，迟到 ACK/NACK 不得影响新 claim（有测试证明）。
11. **ZCode 集成轮裁决（2026-09-12 晚）**：(a) zcode `sessionGateEvent='PreToolUse'`（7 事件含 PreToolUse、exit 2=阻断已实证，与 kimi 同构）；(b) 新增 adapter 契约字段 `deliveryEvents`（回流消费窗口），zcode 的 PreToolUse 仅门禁不做回流——ZCode 对 PreToolUse stdout additionalContext 的注入行为未经实证，claim 后可能被宿主丢弃，pending completion 由紧随的 PostToolUse 即时接管；(c) zcode `blockDeliveryEvents` 为空集（投递全走 stdout 严格 JSON + exit 0，Stop 续轮由 envelope `decision:block` 表达）；(d) 安装器 `localizeMcpConfig` 改为保留插件源 args（原实现硬编码 `[cliPath,'mcp']` 会丢 `--host`）；(e) 插件版本维持 0.1.0 不 bump（CLI 重装幂等，版本升级留到发布）；(f) zcode gate veto 文案写 stderr + exit 2（阻断语义已实证，stderr 对模型的可见性待真实 E2E 确认）。 (g) 修复安装器 CODEX_BIN 探测 bug：zcode-plugin 的 `isExecutable` 为 async，共享发现逻辑同步取真值恒真，导致返回第一个 PATH 候选（可指向不存在的文件）；改用共享同步 accessSync 检查并有回归测试。Kimi 安装器不写 CODEX_BIN，未受影响。
12. **Kimi 真实 E2E hook 失效根因与修复（2026-09-12 晚，探针实证）**：(a) 症状：真实 Kimi 会话中本插件 PreToolUse/Stop/UserPromptSubmit 三 hook 全部未执行（探针 completion 不被消费、current_sessions 恒空、CAS 调用报 `session_not_established`），而 MCP 与 systemPrompt 注入正常；nowledge-mem 插件 hook 同期正常。(b) 排除项：manifest 解析（本地复刻宿主 parseManifest：state=ok、hooks=3、零 diagnostics；author 字符串经 `readAuthor` 宽松接受，早期 zod schema 假设是 API 层另一套，非 record 构建路径）；hook stdin 字段为 snake_case（`toHookInputData` camelToSnake 实证，与 adapter 一致）；插件名正则、`.strict()` hook schema、多 active daemon、TUI /reload（只重载 config/tui.toml 不重载插件）。(c) 实证：宿主 hook 执行为 `spawn(command, [], {shell:true, cwd:插件根, env:{KIMI_CODE_HOME, KIMI_PLUGIN_ROOT}})`，异常静默降级 allow；touch 探针（无引号命令）触发成功，带单引号路径的 node 命令零进程痕迹——单引号引用路径在宿主 Bun shell 下解析失败。(d) 修复：安装器 `quoteShell` 仅对含空白/特殊字符的路径加引号转义，安全字符集裸路径直出；重装后产物命令无引号。npm test fail 0、lint、smoke 通过。(e) 宿主 hook 语义记录：exit 2=block（reason=stderr）、exit 0+stdout JSON 可结构化 block、非 0 非 2=allow；hook stdin 含 `hook_event_name/session_id/cwd/tool_name/tool_input/tool_call_id`；`/reload` 不重载插件，hooks 变更需重启 TUI 或 /plugins disable→enable。
13. **Hook 命令形态终裁：sh wrapper（2026-09-12 深夜，第三次重启后实证修正决策 12）**：(a) 决策 12 的"无引号修复"不完整——无引号 node 绝对路径多参数命令在第三次重启后仍零效果（current_sessions 未被 hook 更新、pending completion 历经 5 次 MCP 调用未 drain），而 knowledge-mem 同结构 hooks 正常、手动 `printf stdin | node main.mjs hook` 完全正常（回流 `<codex-completion>` + exit 2 语义实证）→ 命令健康，收敛为宿主对命令形态的处理差异。(b) 宿主二进制逆向佐证：`runHook` = `hostProcess.spawn(command, [], {shell:true, cwd, env})` + 失败静默 `allowResult`；manifest 解析（parseManifest 手写解析器 + HookDefSchema.strict）对全部命令形态均合规。(c) 终裁：hook command 一律由安装器本地化为 `sh <installPath>/hook-wrapper.sh`（与宿主 config.toml `[[hooks]]` 长期可靠的 `sh /abs/script.sh` 形态一致），wrapper 由 `buildHookWrapper()` 生成（`exec <node> <cli> hook --host=kimi-code "$@"`，标准 sh 引号语义，stdin 经 exec 透传）；`localizeKimiHooks(manifest, {installPath})` 新签名——保留此形态（比裸命令多一层宿主已验证的执行路径）。(d) 矩阵探针污染事实：矩阵测试曾向真实库 current_sessions 写入 `sess-probe`，导致本轮 spawn 的 4 条 completion 错误归属、回流按 session 谓词不匹配——已全部更正；**测试脚本严禁再指向真实库**。(e) ~~Kimi 宿主 MCP 客户端把数组参数序列化为字符串~~ **归因修正（2026-09-13，源码级排查）**：宿主校验为 Ajv（`validateExecutableToolArgs`，错误文案全是 Ajv 默认文案），对真数组**放行**（实验实锤）；出站 schema 五 provider 全部直发（Kimi normalizer 对 anyOf 无损），入站 arguments 纯字符串拼接→JSON.parse→Ajv，本地链路无任何串化点——串化发生在**模型生成的 tool_call arguments**（当前模型在 anyOf 形状上把数组写成 JSON 字符串）。**裁决：不改 schema**（schema 纯净、`"all"` 覆盖高频场景、模型行为是变量）；备用方案已定稿备用——threads 加非空字符串分支 + server 端 JSON.parse 还原（向后兼容纯放宽），按需启用。`codex_wait_many` 数组分支在会串化的模型下不可用登记为已知限制。(f) `kimi plugins list` CLI 子命令已不存在（GUI-only），AGENTS.md SOP 该表述待更新。
14. **Hook 全哑真根因：installed.json source 枚举非法（2026-09-13，用户 /plugins 报错实锤，修正决策 12/13 归因）**：(a) 用户在 TUI 执行 `/plugins` 直接报错：`Failed to load plugins: output validation failed for pluginService.listPlugins: 0.source: Invalid option: expected one of "local-path"|"zip-url"|"github"`——安装器写入 installed.json 的 `source: "plugins/kimi-code"` 不在宿主枚举内，**pluginService 整个插件列表加载校验失败，hooks 被整体静默丢弃**（无任何日志；MCP 走用户级 mcp.json 不受影响、systemPrompt 注入路径宽容不受影响，故此前三态并存极具迷惑性）。(b) 修复：安装器 record 改写 `source: 'local-path'` + `originalSource: 'plugins/kimi-code'`（来源可追溯），单测断言同步；重装后 installed.json 已验证。(c) 归因修正：决策 12 的"touch 探针实证触发"不成立（/tmp 标记文件创建来源无法归因到宿主 hook，大概率是本地验证时自建）——**hooks 从首次安装起就从未在宿主内执行过**；决策 13 的"命令形态"是伴生假设（sh wrapper 保留为稳妥形态，但非根因）。(d) 域名教训：宿主对插件系统的失败是**完全静默的**（输出校验失败不落日志、不影响 MCP/systemPrompt），插件类排障第一入口是 TUI `/plugins` 与 `/plugins info <id>`；详情页渲染不含 Hooks 区块（渲染遗漏，hookCount 有数据不显示），诊断看 Diagnostics 区块。(e) zcode 安装器 `source: 'plugins/zcode'` 存在同构风险，但 ZCode 宿主的 installed.json 枚举独立未取证，留待 ZCode 真实 E2E 轮处理（登记看板）。——**(e) 已于 2026-09-13 取证关闭**：ZCode 宿主 `isInstalledPluginRecord` 为宽松手写 filter（id/installPath/name 三字段 string 检查），无 source 枚举校验，同构风险不成立。
15. **Hook 通道身份对齐双 bug 修复 + 真实宿主全链路首次打通（2026-09-13 晨，wrapper 裸探针 + 真实 stdin 实锤）**：(a) source 修复后 hooks 已被宿主加载（/plugins 详情 state ok、无 diagnostics；列表/详情页均不渲染 hooks 数量属宿主渲染遗漏），但 drain/gate 仍无痕迹——在宿主侧 wrapper 加裸观测日志并让用户按面板 R reload 后 hook 开始执行，抓到真实 stdin 实锤两个对齐 bug：**①宿主 hook stdin 的 session_id 带前缀**（`session_<uuid>`，knowledge-mem 同款原样值），库中为裸 uuid（历史手写+探针污染），drain/gate 谓词按精确匹配永不命中；**②宿主 tool_name 送 MCP 完整限定名**（`mcp__codex-as-subagent__codex_models`），gate 判定用裸名集合 `has()` 恒 false——gate 从未触发，current_sessions 从未被建立。(b) 修复：清理库中脏 session 标识（裸 uuid 行删除/归属统一为带前缀形态）+ `src/cli/hook.mjs` 增加 `casToolName()` 归一化（MCP 通用惯例，非 host-specific；取 `__` 末段匹配 CAS 集合，非限定名原样返回）+ hook-gate 单测；hook CLI 每事件现起进程读源码，修复即时生效无需重启。(c) **真实验证全通过**：真实 TUI 内 spawn → PreToolUse gate 建立 `current_sessions`（session_731af287 形态）→ completion 正确归属 → 下一工具调用被 veto + `<codex-completion>` 注入（11+22=33）→ `delivery_state=delivered` 落库；手动宿主式调用亦一次注入 5 条积压 completion（含 interrupted 状态）验证 drain 语义。(d) 域名事实登记：**session 身份唯一权威来源是 hook stdin 原样值**，MCP 侧一律经 current_sessions 读取（无独立获取途径），两通道表示天然一致，禁止在任何一层对 session_id 做格式改写或猜测。(e) 宿主 reload：/plugins 面板 **R 键**即重载插件与 hooks（`Reload: +0 -0` 为增删计数，非成功标志），manifest/wrapper 改动经 reload 即时生效，无需重启 TUI。
16. **thread_held 投影丢 data 修复（2026-09-13，ZCode 跨 Host 隔离 E2E 发现，主会话直接修补）**：(a) 症状：真实跨 Host 探测（kimi 会话对 zcode-held 运行中线程做 status/read_thread/send/steer/interrupt/wait 六类操作）全部只返回 `{code:"thread_held",message}`，无 `data.holderHost`。(b) 根因：§3.10 修复只落到 Runtime Server 路由层（request-router.mjs:101 保留 error.data），MCP 最后一跳 `toolErrorContent`（stdio-bootstrap.mjs）仍只投影 `{code,message}` 把 data 丢弃——测试断层：core/integration 层有 holderHost 断言、bootstrap 投影层零覆盖。(c) 修复：toolErrorContent 透传 `error.data` + 新增 2 单测（socket 序列化错误带 data、throw ThreadHeldError 两路径）；回归 223/223。(d) 口径澄清：用户现场曾解读为"fail-closed 不泄漏 holder 是设计"——与规格不符：实施规格 §3.10 明确"MCP/内部错误投影必须保留 error.data"，固定形状含 `data.holderHost`；决策 6/9 定义 holderHost 为 thread_held 错误面组成部分，仅"无法确定唯一 holder"时省略；AGENTS.md 禁止泄漏清单（turnId/event cursor/workspace/approval/raw events/delivery 内部字段）不含 holderHost。(e) 真实路径复验需新 ZCode 会话（MCP 进程每会话新建，本会话 bootstrap 为旧代码），与 Phase B 反向探测合并执行。

## 执行边界

- 主 Agent 负责需求收敛、任务排序、集成、复核（本轮取消独立 Review Agent）、最终验证与用户级验收；验收记录写入 docs/autonomous-runs/。
- 子 Agent 按计划分配互不重叠的写入范围，不创建新的子 Agent，不修改三份 V2 规格文档与 V1 基线文档。
- 所有当前阶段更新写入本文件；稳定使用说明写入 README.md；通用规则写入 AGENTS.md。

## 完成定义（本轮 V2）

三份 V2 规格全部实现并通过确定性验收（含实施规格 §3.13 全部场景与架构设计 §十 Recovery Matrix 的可测行）：V2 schema 重建、Host-scope Store、Session Gate、Thread Hold/Presence、`thread_held`/`session_not_established` 错误面、hook/drain/mcp 新 CLI 协议、Kimi TUI PreToolUse 双职责、Hook-only Mailbox（四态 claim 状态机 + Terminal Initiate 两分支 + claim 层恢复）、全部 Web transport 移除、Kimi 插件与安装器更新；npm test/lint/smoke 全绿；真实 Kimi TUI E2E 与 ZCode 插件集成按看板留给用户或下一轮。
